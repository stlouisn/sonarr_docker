version: '2.4'

services:

  sonarr:
    image: stlouisn/sonarr
    container_name: sonarr
    hostname: sonarr
    environment:
      - LC_ALL=C.UTF-8
      - TZ=UTC
    restart: always
    volumes:
      - /docker/sonarr/config:/var/lib/sonarr:rw
      - /media/tvshows:/tv:rw
    ports:
      - 8989
